---
description: 本仓库 Cursor / 客户端走奇绩 OpenAI-next 两套协议时的边界
alwaysApply: true
---

# 奇绩 API（仅本仓库 + 隔壁 Brainlink）

两套协议（同一把 Key）：

| 协议 | Base URL | 用途 |
|------|----------|------|
| OpenAI 兼容 | `https://api.openai-next.com/v1`（**要** `/v1`） | Cursor BYOK、Codex、本仓 LLM 客户端 |
| Anthropic 原生 | `https://api.openai-next.com`（**不要** `/v1`） | Claude Code / Messages API；客户端会拼 `/v1/messages` |

**产品分发（用户 2026-09-05 授权）：** 运行时奇绩 Key 已写入可提交的 `config/llm-apis.md`，下载本产品的用户可用该通道。Kimi / MiniMax / Qwen 等其它厂商仍需用户自备。

仍禁止：
- 把 Key 写进 Cursor **全局** Settings → Models（会泄漏到其它项目）
- 在 NoAI-Lab、AB-TEST 等未授权仓库使用
- 把原始脑电 / 可识别健康信息送进该 API

Cursor 本机 BYOK 仍可用 gitignore 的 `.cursor/settings.local.json`；工作区无密钥 URL 在 `.cursor/settings.json`。
Cursor 订阅模型（Composer / Grok 等）不吃自定义 Key。操作说明见 `docs/agent-lab/qiji-cursor-howto.md`。
